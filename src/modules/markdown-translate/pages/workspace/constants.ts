/**
 * 工作区页面级常量。
 *
 * 例如首屏示例 Markdown：让用户打开页面即可看到可转换内容，
 * 降低首次使用门槛。
 */

export const EXAMPLE_MARKDOWN = `# Understanding React Hooks

React Hooks let you use state and lifecycle features in function components.

\`\`\`ts
const [count, setCount] = useState(0);
\`\`\`

- reusable logic
- component state
`;
